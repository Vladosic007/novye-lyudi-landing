# Как править сайт (без знания кода)

Весь текст лежит в файле **`index.html`**. Открой его в любом редакторе
(подойдёт даже «Блокнот», но удобнее [VS Code](https://code.visualstudio.com/)).

## Главное правило
Меняй только текст **между** тегами. Сами теги — угловые скобки `<...>` — не трогай.

```
<h1>Против блокировок интернета</h1>
        ↑ меняешь только это ↑
```

Все места, которые можно менять, помечены комментарием **`✏️ ТЕКСТ`** — ищи их
поиском (Ctrl+F → `✏️`).

---

## Частые задачи

### 📝 Поменять заголовок, подзаголовок, тексты
Найди нужный блок по метке `✏️ ТЕКСТ` и впиши свой текст.

### 🎥 Добавить видео
1. Положи файл в `assets/video/hero.mp4` (и картинку-постер в `assets/img/hero-poster.jpg`).
2. В `index.html` найди блок «HERO + ВИДЕО».
3. Раскомментируй блок `<video>` (убери `<!--` и `-->` вокруг него).
4. Удали блок `<div class="video-placeholder">…</div>` (заглушку).

### 👥 Добавить/поменять человека в команде
- **Текст:** найди секцию «КОМАНДА», меняй `Имя Фамилия`, `Роль в команде`, описание.
- **Фото:** положи фото в `assets/img/team/`, затем внутри `member__photo`
  замени `<svg>…</svg>` на `<img src="assets/img/team/имя.jpg" alt="Имя Фамилия" />`.
- **Ещё карточка:** скопируй целиком один блок `<article class="member">…</article>`
  и вставь рядом.

### 🔗 Соцсети и контакты
- Соцсети — секция подвала, метка `✏️ СОЦСЕТИ`: подставь ссылки в `href="#"`.
- Почта и телефон — метка `✏️ ТЕКСТ` в подвале.

### 🎨 Поменять цвета
Открой `assets/css/styles.css`, самый верх — блок `:root`. Меняй значения
`--brand`, `--cta` и т.д. (это фирменная бирюза партии).

---

## Как увидеть изменения
Просто открой `index.html` двойным кликом в браузере. Форма по-настоящему
заработает только после публикации на Vercel (см. `README.md`).
